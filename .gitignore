# Node
node_modules/
.pnp
.pnp.js

# build output
dist
dist-ssr
build

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs
*.log

# editor / os
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
*.suo
*.sw?

# env
.env
.env.local
.env.*.local

# cache
.cache
.temp
*.tsbuildinfo
.eslintcache

# Generated into public/ so the dev server serves them, but not tracked:
# RSS embeds a <lastBuildDate>, so committing it would diff on every build.
# Both are rewritten by `predev` and `prebuild`, so a fresh clone is fine.
public/feeds/rss.xml
public/feeds/sitemap.xml

